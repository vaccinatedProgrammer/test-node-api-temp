/**
 * Define mocks to seed the local docker-compose MongoDB instance.
 *
 * Usage:
 * - keys = collection names
 * - values = arrays of documents
 *
 * The seed command will create the collections and insert the documents.
 *
 * Example:
 *
 * module.exports = {
 *   testCollection: [
 *     {
 *       id: 1,
 *       whatever: true
 *     },
 *     {
 *       id: 2,
 *       whatever: false
 *     }
 *   ]
 * }
 *
 */

module.exports = {};
